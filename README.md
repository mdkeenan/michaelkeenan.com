# Minimal Dark Site

Minimal static HTML + CSS layout with:

- Left navigation pane
- Top banner area
- Content area

## Run

Open `index.html` in your browser.

Or from PowerShell (optional):

```powershell
cd .\minimal-dark-site
python -m http.server 8000
```

Then visit `http://localhost:8000`.
